# Semantic Search Engine

This project is currently a work in progress.

The core pipeline and evaluation setup are still being improved, and more updates will be added over time.

Note:

- `config.py` is intentionally not included at this stage for further changes and flexibility.
